<UserControl x:Class="QuotePad.View.AuthorSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:QuotePad.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="576">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <view:AuthorListView Grid.Column="0" DataContext="{Binding ItemListViewModel}" Margin="0,0,5,0" />
        <GridSplitter ResizeDirection="Columns" VerticalAlignment="Stretch" Width="4" ShowsPreview="True" />
        <view:AuthorView Grid.Column="1" DataContext="{Binding CurrentSelection}" 
                         IsEditMode="{Binding RelativeSource={RelativeSource AncestorType={x:Type view:AuthorSetupView}},Path=DataContext.IsEditMode}" />
    </Grid>
</UserControl>
